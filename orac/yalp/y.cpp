#include <bits/stdc++.h>

using namespace std;

int main() {
    int R,C;cin>>R>>C;
    using pii=pair<int,int>;
    vector<pii> empty;
    vector<vector<bool>>op(R+1,vector<bool>(C+1));
    for (int i=1;i<=R;i++){
        string s;cin>>s;
        for (int j=1;j<=C;j++){
            if(s[j-1]!='*'){
            }
        }
    }
}
