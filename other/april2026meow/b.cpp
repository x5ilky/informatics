#include <bits/stdc++.h>
using namespace std;
#define int long long
signed main() {
    const int N=100;
    vector<vector<int>>dp(N+1,vector<int>(N+1));
    for(int i=1;i<=N;i++){
        dp[1][i]=1;
    }
    for(int i=1;i<=N;i++){
    }
}
